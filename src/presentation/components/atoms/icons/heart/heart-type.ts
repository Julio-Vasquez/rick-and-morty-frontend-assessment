export interface HeartProps {
  filled?: boolean // true => lleno; false => contorno
  size?: number // px (altura/anchura)
  className?: string // clases extra para el <svg>
  title?: string // accesibilidad
}
